This projects aims to streamline the prior authorization (PA) process.

The first set of measures simplify the preliminary steps for the PA, namely, the database report, filtering fields, breaking it down into separate sheets, and merging it with existing data
These enhancements includes a number of SQL Server queries, a resulset parser coded in Perl and a batch job in the windows scheduler.



